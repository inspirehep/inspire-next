{
    "$schema": "http://json-schema.org/schema#",
    "properties": {
        "$ref": {
            "description": "URL to the referenced resource",
            "format": "url",
            "type": "string"
        }
    },
    "required": [
        "$ref"
    ],
    "type": "object"
}
